version: 1.0.{build}

image: Visual Studio 2022

build_script:
  - ps: |
      Write-Host "🔑 Thiết lập mật khẩu cho Administrator..."
      $password = ConvertTo-SecureString "Xy!9#2025_RdpStrong*" -AsPlainText -Force
      Set-LocalUser -Name "Administrator" -Password $password
      Enable-LocalUser -Name "Administrator"

      Write-Host "🚀 Mở tunnel Pinggy SSH (RDP 3389)..."
      Start-Process -NoNewWindow -FilePath "ssh.exe" -ArgumentList "-p 443 -R0:localhost:3389 -o StrictHostKeyChecking=no -o ServerAliveInterval=30 lVuaQjAzw82+tcp@free.pinggy.io"

      Write-Host "⏳ Giữ kết nối..."
      for ($i=0; $i -lt 60; $i++) {
        Start-Sleep -Seconds 60
        Write-Host "Đang giữ: $i phút"
      }
